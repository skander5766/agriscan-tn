[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python3.11 -m uvicorn main:app --host 0.0.0.0 --port $PORT & python3.11 telegram_bot.py"
